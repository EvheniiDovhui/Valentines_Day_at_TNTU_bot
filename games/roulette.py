import asyncio
from aiogram import types

async def play_roulette(message: types.Message):
    # Надсилаємо анімацію казино
    dice_msg = await message.answer_dice(emoji="🎰")
    value = dice_msg.dice.value
    
    # Чекаємо 3.5 секунди, поки барабан візуально зупиниться
    await asyncio.sleep(3.5)
    
    # ПЕРЕВІРКА: 
    # 1 - це "777" (Джекпот)
    # 22 - три виногради
    # 43 - три лимони
    if value == 1:
        await message.answer(
            "🎉 <b>ДЖЕКПОТ!</b>\n\nСьогодні Амур на твоєму боці! Твоє кохання зовсім поруч! ❤️", 
            parse_mode="HTML"
        )
    elif value in [22, 43]:
        await message.answer(
            "✨ <b>Майже джекпот!</b>\n\nТобі сьогодні неабияк щастить! Можливо, час написати комусь першим?", 
            parse_mode="HTML"
        )
    else:
        await message.answer(
            "😢 <b>Не цього разу...</b>\n\nАле пам'ятай: не щастить у грі — пощастить у коханні! ❤️", 
            parse_mode="HTML"
        )